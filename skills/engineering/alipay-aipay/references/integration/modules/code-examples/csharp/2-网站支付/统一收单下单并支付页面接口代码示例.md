# 统一收单下单并支付页面接口 - C# 示例

## 请求示例

```csharp
using Aop.Api;
using Aop.Api.Request;
using Aop.Api.Response;
using Aop.Api.Domain;

// 初始化SDK（C#/.NET 使用 PKCS#1 格式私钥，Java 使用 PKCS#8 格式，请勿混用）
IAopClient alipayClient = new DefaultAopClient(GetAlipayConfig());

var request = new AlipayTradePagePayRequest();
var notifyUrl = Environment.GetEnvironmentVariable("ALIPAY_NOTIFY_URL");
if (!string.IsNullOrWhiteSpace(notifyUrl))
{
    request.SetNotifyUrl(notifyUrl.Trim());  // 真实生产或公网联调时传入；本地没有公网 HTTPS notify_url 时不要传 null、空字符串或占位 URL
}
request.SetReturnUrl("https://your-domain.com/return");  // 默认传入并替换为已验证结果页；仅用户明确关闭同步回跳时省略

var model = new AlipayTradePagePayModel
{
    OutTradeNo = "MERCHANT_ORDER_NO_PLACEHOLDER",             // 商户订单号，必填
    TotalAmount = "88.88",                        // 订单总金额，必填，单位为元
    Subject = "Iphone6 16G",                      // 订单标题，必填
    ProductCode = "FAST_INSTANT_TRADE_PAY",       // 网站支付固定值，必填
};
request.SetBizModel(model);

// ⚠️ 网站支付必须使用 PageExecute()，不能使用 Execute()
// ⚠️ 第二个参数传 null（accessToken），第三个参数 "POST" 表示生成 POST 表单
AlipayTradePagePayResponse response = alipayClient.PageExecute(request, null, "POST");
string formHtml = response.Body;
// PageExecute 返回的是 HTML 表单字符串，前端需要渲染并自动提交

private static AlipayConfig GetAlipayConfig()
{
    string privateKey  = "<-- 请填写您的应用私钥，例如：MIIE...（脱敏示例） -->";
    string alipayPublicKey = "<-- 请填写您的支付宝公钥，例如：MIIB...（脱敏示例） -->";
    AlipayConfig alipayConfig = new AlipayConfig();
    alipayConfig.ServerUrl = "https://openapi.alipay.com/gateway.do";
    // 沙箱环境: alipayConfig.ServerUrl = "https://openapi-sandbox.dl.alipaydev.com/gateway.do";
    alipayConfig.AppId = "<-- 请填写您的AppId，例如：20190xxxxxx -->";
    alipayConfig.PrivateKey = privateKey;
    alipayConfig.Format = "json";
    alipayConfig.AlipayPublicKey = alipayPublicKey;
    alipayConfig.Charset = "UTF-8";
    alipayConfig.SignType = "RSA2";
    return alipayConfig;
}
```

## 响应示例
### 正常示例
```html
<form name="punchout_form" method="post" action="https://openapi.alipay.com/gateway.do?charset=UTF-8&method=alipay.trade.page.pay&format=json&sign=SIGN_PLACEHOLDER&version=1.0&app_id=20190xxxxxx&sign_type=RSA2&timestamp=2014-07-24+03%3A07%3A50">
<input type="hidden" name="biz_content" value="{&quot;out_trade_no&quot;:&quot;MERCHANT_ORDER_NO_PLACEHOLDER&quot;,&quot;total_amount&quot;:&quot;88.88&quot;,&quot;subject&quot;:&quot;Iphone6 16G&quot;,&quot;product_code&quot;:&quot;FAST_INSTANT_TRADE_PAY&quot;}">
<input type="submit" value="立即支付" style="display:none" >
</form>
<script>document.forms[0].submit();</script>
```
