# 统一收单交易退款查询接口 - Java 示例

## 请求示例

```java
package com.java.sdk.demo;

import com.alipay.api.AlipayApiException;
import com.alipay.api.AlipayClient;
import com.alipay.api.DefaultAlipayClient;
import com.alipay.api.AlipayConfig;
import com.alipay.api.domain.AlipayTradeFastpayRefundQueryModel;
import com.alipay.api.response.AlipayTradeFastpayRefundQueryResponse;
import com.alipay.api.request.AlipayTradeFastpayRefundQueryRequest;

public class AlipayTradeFastpayRefundQuery {

    public static void main(String[] args) throws AlipayApiException {
        // 初始化SDK（Java 使用 PKCS#8 格式私钥，C#/Python 使用 PKCS#1 格式，请勿混用）
        AlipayClient alipayClient = new DefaultAlipayClient(getAlipayConfig());

        AlipayTradeFastpayRefundQueryRequest request = new AlipayTradeFastpayRefundQueryRequest();
        AlipayTradeFastpayRefundQueryModel model = new AlipayTradeFastpayRefundQueryModel();
        model.setOutTradeNo("MERCHANT_ORDER_NO_PLACEHOLDER");  // 商户订单号，与 tradeNo 二选一
        model.setOutRequestNo("OUT_REQUEST_NO_PLACEHOLDER");         // 退款请求号，必填，同一笔交易多次部分退款时用于区分
        request.setBizModel(model);

        AlipayTradeFastpayRefundQueryResponse response = alipayClient.execute(request);

        if (response.isSuccess()) {
            // refund_status: REFUND_SUCCESS 表示退款成功，REFUND_PROCESSING 表示退款处理中
            System.out.println("退款状态: " + response.getRefundStatus() + ", 退款金额: " + response.getRefundAmount());
        } else {
            System.out.println("调用失败: " + response.getSubCode() + " - " + response.getSubMsg());
        }
    }

    private static AlipayConfig getAlipayConfig() {
        String privateKey  = "<-- 请填写您的应用私钥，例如：MIIE...（脱敏示例） -->";
        String alipayPublicKey = "<-- 请填写您的支付宝公钥，例如：MIIB...（脱敏示例） -->";
        AlipayConfig alipayConfig = new AlipayConfig();
        alipayConfig.setServerUrl("https://openapi.alipay.com/gateway.do");
        // 沙箱环境: alipayConfig.setServerUrl("https://openapi-sandbox.dl.alipaydev.com/gateway.do");
        alipayConfig.setAppId("<-- 请填写您的AppId，例如：20190xxxxxx -->");
        alipayConfig.setPrivateKey(privateKey);
        alipayConfig.setFormat("json");
        alipayConfig.setAlipayPublicKey(alipayPublicKey);
        alipayConfig.setCharset("UTF-8");
        alipayConfig.setSignType("RSA2");
        return alipayConfig;
    }
}
```

## 响应示例
### 正常示例
```json
{
    "code": "10000",
    "msg": "Success",
    "trade_no": "ALIPAY_TRADE_NO_PLACEHOLDER",
    "out_trade_no": "MERCHANT_ORDER_NO_PLACEHOLDER",
    "out_request_no": "OUT_REQUEST_NO_PLACEHOLDER",
    "total_amount": "100.20",
    "refund_amount": "12.33",
    "refund_status": "REFUND_SUCCESS"
}
```
### 异常示例
```json
{
    "code": "20000",
    "msg": "Service Currently Unavailable",
    "sub_code": "isp.unknow-error",
    "sub_msg": "系统繁忙"
}
```