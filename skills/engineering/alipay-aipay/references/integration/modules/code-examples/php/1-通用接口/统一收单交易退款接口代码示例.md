# 统一收单交易退款接口 - PHP 示例

## 请求示例

```php
<?php
require_once __DIR__ . '/aop/AopClient.php';
require_once __DIR__ . '/aop/request/AlipayTradeRefundRequest.php';

// 初始化 SDK（PHP 使用 PKCS#1 格式私钥，Java 使用 PKCS#8 格式，请勿混用）
$aop = new AopClient();
$aop->gatewayUrl = 'https://openapi.alipay.com/gateway.do';
// 沙箱环境: $aop->gatewayUrl = 'https://openapi-sandbox.dl.alipaydev.com/gateway.do';
$aop->appId = '<-- 请填写您的AppId，例如：20190xxxxxx -->';
$aop->rsaPrivateKey = '<-- 请填写您的应用私钥（PKCS#1格式），例如：MIIE...（脱敏示例） -->';
$aop->alipayrsaPublicKey = '<-- 请填写您的支付宝公钥，例如：MIIB...（脱敏示例） -->';
$aop->format = 'json';
$aop->postCharset = 'UTF-8';
$aop->signType = 'RSA2';

// 构造请求参数
$request = new AlipayTradeRefundRequest();
$bizContent = [
    'out_trade_no' => 'MERCHANT_ORDER_NO_PLACEHOLDER',         // 商户订单号
    // 'trade_no' => 'ALIPAY_TRADE_NO_PLACEHOLDER', // 支付宝交易号，与 out_trade_no 二选一
    'refund_amount' => '0.01',                     // 退款金额
    'refund_reason' => '测试退款',                  // 退款原因
    'out_request_no' => 'RF' . time(),             // 退款请求号，每次退款需不同
];
$request->setBizContent(json_encode($bizContent, JSON_UNESCAPED_UNICODE));

// 调用接口
$response = $aop->execute($request);
$responseApiName = str_replace('.', '_', $request->getApiMethodName()) . '_response';
$result = $response->$responseApiName;

if (!empty($result->code) && $result->code == '10000') {
    $fundChange = $result->fund_change ?? 'N';
    echo "退款状态: " . ($fundChange === 'Y' ? '退款成功' : '未发生资金变化');
} else {
    echo "调用失败: " . $result->sub_code . " - " . $result->sub_msg;
}
```

## 响应示例
### 正常示例
```json
{
    "code": "10000",
    "msg": "Success",
    "trade_no": "ALIPAY_TRADE_NO_PLACEHOLDER",
    "out_trade_no": "MERCHANT_ORDER_NO_PLACEHOLDER",
    "buyer_logon_id": "159****5620",
    "fund_change": "Y",
    "refund_fee": "0.01"
}
```
### 异常示例
```json
{
    "code": "20000",
    "msg": "Service Currently Unavailable",
    "sub_code": "isp.unknow-error",
    "sub_msg": "系统繁忙"
}
```